# This program asks for the number. The program then prints out all integer numbers greater than zero but smaller than the input.

user_number = int(input("Upper Limiit: "))

current = 1
while current < user_number:
    print(current)
    current +=1